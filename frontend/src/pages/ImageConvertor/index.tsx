import React from 'react';
import Form from './Form';
import Select from './Select';

export default function index() {
    return (
        <div>
            <Form/>
            <Select/>
        </div>
    );
}

